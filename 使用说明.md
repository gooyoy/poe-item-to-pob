# POE1 装备转 POB（国服中译英）

把国服 POE1 的**中文装备信息**转成 **POB 能识别的英文**。油猴脚本，装完两个场景都能用。

**纯离线** —— 词库内置在脚本里，不联网、不上传数据、不依赖任何服务器。

---

## 安装（约 1 分钟）

**第 1 步**：装脚本管理器（已装可跳过）

- Chrome / Edge：[Tampermonkey](https://www.tampermonkey.net/)
- Firefox：Tampermonkey 或 Violentmonkey

**第 2 步**：安装脚本

把 `poe-item-to-pob.user.js` **直接拖进浏览器窗口**，Tampermonkey 会弹出安装页，点「安装」即可。

> 脚本约 5.3 MB（因为内置了完整词库），安装时会卡一两秒，正常现象。装完就是本地的，以后打开秒开。

**第 3 步**：验证

打开 [国服市集](https://poe.game.qq.com/trade)，右下角出现金色 **POB** 圆球就说明生效了。

---

## 两个使用场景

### 场景一：官网市集

打开市集搜索页，每个格子**左下角的图标组**（加入收藏 / 按属性筛选）旁边会出现 **⧉ POB** 按钮。

那组图标是鼠标移入格子才显示的，但 POB 按钮**始终可见**。定位方式不依赖类名和坐标，而是靠一个独有特征识别那个容器：**它内部只有图标、没有任何文字**（价格区、账号区、「忽略玩家」都带文字）。按钮以正常文档流追加，不用绝对定位，所以不会压住任何内容。

点一下 → 自动复制成英文 → 去 POB 的 **Items** 页按 `Ctrl+V`。

按钮会即时反馈：`✓ 已复制` 表示成功；若显示 `见面板` 则是浏览器拦截了自动复制，此时会自动弹出面板供你手动复制。

> 按钮采用绝对定位挂在结果行上，不插入原有文字节点 —— 这样翻页、筛选时不会被页面重渲染冲掉。

### 场景二：游戏里复制的装备

1. 游戏内鼠标悬停装备，按 `Ctrl+C`
2. 浏览器切到任意 POE 页面，按 **`Alt+V`**（或点右下角 POB 球）
3. 在左栏按 `Ctrl+V` 粘贴 —— **自动转换，不用点按钮**
4. 点「复制结果」→ 去 POB 按 `Ctrl+V`

面板里还有「读取剪贴板」按钮，可以一键把剪贴板内容拉进来。

---

## 它怎么保证不认错装备

按钮定位**完全不依赖 CSS 类名**（国服类名与国际服不同，靠类名会整片失效）：

| 环节 | 做法 |
| --- | --- |
| 找物品行 | 以每件装备都有的「物品等级」文字为锚点，向上定位到行容器 |
| 配对数据 | 用「物品等级 + 名称/基底 + 词缀原文」做指纹匹配 |
| 取数通道 | 优先用拦截到的 API JSON；拿不到时回退解析页面文字 |

词缀中文原文在 API 和页面上完全一致，所以即使**同名同基底的装备并排出现**，也能各自配对到自己的数据。已实测：两件异教法杖（ilvl 85 / 78）各自输出正确的词缀数值，不串味。

API JSON 通道拿到的是结构化数据，插槽连接（`Sockets: G-G-R B`）、影响标记（`Shaper Item`）、词缀分类（`(implicit)` / `(crafted)` / `(enchant)`）都能准确还原。

---

## 翻译能力：三层兜底

| 层 | 来源 | 作用 |
| --- | --- | --- |
| ① 内核 | `cn-poe-translator`（MIT） | 主力，绝大多数常见词缀 |
| ② 人工规则 | 70 条 | 修内核已知缺陷 |
| ③ 兜底词库 | **36,347 条** | 从 PoeCharm 汉化词典生成，覆盖冷门词缀 |

第 ③ 层是关键。官方词库停更在 3.27 且仓库已归档，而 PoeCharm 的汉化词典是随补丁更新的，把它反向索引成「中文→英文」就能捞回大量内核不认识的词缀。

实测例子：`图腾所使用的技能击中时，有 8% 的几率嘲讽敌人` 内核完全不认识，兜底层准确翻出 `Skills used by Totems have a 8% chance to Taunt on Hit`。

**已修正的内核缺陷**（都是实测发现的）：

- 传奇物品名被替换成 `Item`，导致 POB 认不出独有词缀 → 已用 1309 个传奇的反查索引修复
- `物品类别: 头盔` / `药剂` 不翻译 → 已修
- `品质(属性词缀)` 等标签不翻译 → 已修
- 词库只收全角冒号 `攻击暴击率：`，而游戏文本两种都有 → 已兼容
- `武器范围：11 米` 单位未转换 → 已修为 `metres`

---

## 遇到翻不出来的词缀

面板下方会用黄框列出来，**不会让你悄悄导入错数据**。

**临时处理** —— 去 [poedb](https://poedb.tw/cn/) 查这条词缀，把页面语言从 `cn` 切到 `us` 拿英文，手动替换那一行（记得把数值改成实际数字）。

**永久解决** —— 需要重新打包，见下方「二次开发」。

---

## 已知边界

**装备自定义名保留中文** —— 比如你自己起名的稀有装备。这不影响 POB 计算（POB 只读基底类型和词缀），介意的话手动改掉。传奇物品名会正确翻译成英文，不受此影响。

**市集 DOM 结构若大改** —— 按钮可能不出现，但 API 拦截仍然有效：按 `Alt+V` 打开面板，用「本页市集物品」列表照样能取装备。面板里的「诊断」按钮可生成结构信息用于反馈修复。

**只支持 POE1 国服**。POE2 请用 [poe2db 的 PobItem](https://poe2db.tw/cn/PobItem)。

---

## 分享给朋友

直接把 `poe-item-to-pob.user.js` 这一个文件发给他就行 —— 拖进浏览器即装，不需要 Node、不需要联网、不需要配置。

想挂到网上让人一键安装，可以传到 [GreasyFork](https://greasyfork.org/)（免费，支持自动更新）或 GitHub Gist 的 raw 链接。

---

## 二次开发

源码在 `E:\PoeCharm\PobItemTool\`：

```
src/core.js      逻辑层（JSON→POB、文本→POB），无 DOM 依赖
src/ui.js        界面与市集注入
patch.js         人工规则（想加词条改这里）
build.js         打包成 .user.js
build-extra-db.js  从 PoeCharm 词典重新生成兜底词库
```

常用命令：

```bash
node src/test-core.js     # 逻辑层单测
node test-bundle.js       # 验证打包产物能否运行
node build.js             # 重新打包
node build-extra-db.js    # PoeCharm 汉化更新后刷新词库
```

加一条翻译规则（改 `patch.js` 后跑 `node build.js`）：

```javascript
// 带数字的词缀
[/^某个新词缀提高 (\d+)%$/u, "$1% increased Something"],

// 固定文本
["某个中文词条", "Some English Text"],
```

本地还有个网页版 `index.html`（不依赖浏览器插件），双击 `启动工具.bat` 即用。

---

## 许可与致谢

- 翻译内核：[cn-poe-translator](https://github.com/cn-poe-community/cn-poe-translator) — MIT
- 词库：[cn-poe-export-db](https://github.com/cn-poe-community/cn-poe-export-db) — 上游已归档，故本地化保存
- 兜底词库源：PoeCharm 汉化词典
- 按钮注入方案参考：[poe2-trade-pob](https://github.com/gogit2194/poe2-trade-pob)（POE2 版，其行选择器与绝对定位思路很稳）

本脚本 MIT 许可。不修改游戏文件、不注入游戏进程，仅在浏览器内做文本转换。
